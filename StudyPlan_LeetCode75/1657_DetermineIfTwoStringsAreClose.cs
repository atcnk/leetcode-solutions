/* get length of strings, if not equal it is impossible to attain these words, return false
 * 
 */

public class Solution
{
    public bool CloseStrings(string word1, string word2)
    {
        int w1l = word1.Length, w2l = word2.Length;

        if (w1l != w2l) return false;


    }
}